name: RDP (Fast Malware Sandbox - No Bloat)

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Configure Core RDP Settings
        run: |
          # Disable NLA and Audio Capture restrictions (Registry changes are instant)
          Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services' -Name "fDisableAudioCapture" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services' -Name "MaxDisconnectionTime" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force

          # Firewall Rule
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389

      - name: Create RDP User
        env:
          MY_SECRET_PASS: ${{ secrets.PASS }}
        run: |
          if (-not $env:MY_SECRET_PASS) {
              Write-Error "Error: Secret 'PASS' is missing."
              exit 1
          }
          $securePass = ConvertTo-SecureString $env:MY_SECRET_PASS -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          echo "RDP_CREDS=User: RDP | Password: (Hidden)" >> $env:GITHUB_ENV

      - name: Install Brave & Security Tools
        run: |
          Write-Host "Installing Lightweight Security Suite..."

          # 1. ESSENTIALS (Fastest install flags)
          # WinRAR (for unpacking malware)
          choco install winrar -y --no-progress
          # Brave (Safe browsing)
          choco install brave -y --no-progress

          # 2. SECURITY & ANALYSIS TOOLS
          # Process Hacker: Replaces Task Manager, shows hidden connections/processes
          choco install processhacker -y --no-progress
          
          # VirusTotal Uploader: Right-click check files
          choco install virustotaluploader -y --no-progress
          
          # Sysinternals Suite: Contains Autoruns, TCPView, ProcMon (Essential for analysis)
          choco install sysinternals -y --no-progress

          # 3. CLEANUP & CONFIGURATION
          
          # Fix Brave Path (Move to Public for easier access)
          $BraveAdmin = "C:\Users\runneradmin\AppData\Local\BraveSoftware"
          if (Test-Path $BraveAdmin) {
              Copy-Item -Path $BraveAdmin -Destination "C:\" -Recurse -Force
          }

          # 4. CREATE SHORTCUTS
          $WshShell = New-Object -comObject WScript.Shell
          
          # Brave Shortcut
          $BraveLnk = $WshShell.CreateShortcut("C:\Users\Public\Desktop\Brave.lnk")
          $BraveLnk.TargetPath = "C:\BraveSoftware\Brave-Browser\Application\brave.exe"
          $BraveLnk.Save()

          # Process Hacker Shortcut
          $PHLnk = $WshShell.CreateShortcut("C:\Users\Public\Desktop\Process Hacker.lnk")
          $PHLnk.TargetPath = "C:\Program Files\Process Hacker 2\ProcessHacker.exe"
          $PHLnk.Save()
          
          # TCPView Shortcut (To see virus network connections)
          $TCPLnk = $WshShell.CreateShortcut("C:\Users\Public\Desktop\TCPView.lnk")
          $TCPLnk.TargetPath = "C:\ProgramData\chocolatey\lib\sysinternals\tools\Tcpview.exe"
          $TCPLnk.Save()

          # Autoruns Shortcut (To see how virus starts with Windows)
          $AutoLnk = $WshShell.CreateShortcut("C:\Users\Public\Desktop\Autoruns.lnk")
          $AutoLnk.TargetPath = "C:\ProgramData\chocolatey\lib\sysinternals\tools\Autoruns64.exe"
          $AutoLnk.Save()

          # VirusTotal Shortcut
          $VTLnk = $WshShell.CreateShortcut("C:\Users\Public\Desktop\VirusTotal Uploader.lnk")
          $VTLnk.TargetPath = "C:\Program Files (x86)\VirusTotalUploader2\VirusTotalUploader2.exe"
          $VTLnk.Save()

          Write-Host "Security Sandbox Ready."

      - name: Start Bore & Keep Alive
        run: |
          Invoke-WebRequest -Uri "https://github.com/ekzhang/bore/releases/download/v0.5.0/bore-v0.5.0-x86_64-pc-windows-msvc.zip" -OutFile "bore.zip"
          Expand-Archive bore.zip -DestinationPath .
          
          Start-Process -FilePath ".\bore.exe" -ArgumentList "local 3389 --to bore.pub" -RedirectStandardOutput "bore.log" -PassThru
          
          Write-Host "Scanning for connection..."
          $address = $null
          $i = 0
          while ($i -lt 150) { 
              if (Test-Path "bore.log") {
                  $content = Get-Content "bore.log"
                  $match = $content | Select-String "listening at (bore.pub:[0-9]+)"
                  if ($match) {
                      $address = $match.Matches.Groups[1].Value
                      break
                  }
              }
              Start-Sleep -Seconds 2
              $i++
          }
          
          if (-not $address) {
             Write-Error "Failed to get Bore address."
             exit 1
          }
          
          Write-Host "`n=== SANDBOX READY ==="
          Write-Host "Address: $address"
          Write-Host "Username: RDP"
          Write-Host "Password: $(echo $env:RDP_CREDS)"
          Write-Host "=====================`n"
          
          while ($true) {
              Write-Host "[$(Get-Date)] Monitoring..."
              Start-Sleep -Seconds 300
          }
